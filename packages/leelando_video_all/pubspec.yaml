name: leelando_video_all
description: 集成全部可选播放内核的 leelando_video 全量便捷包，适合需要一次性启用所有原生播放引擎的应用。
version: 0.3.0
resolution: workspace
repository: https://github.com/LeelandoAi/Flutter_Video/tree/main/packages/leelando_video_all
issue_tracker: https://github.com/LeelandoAi/Flutter_Video/issues

environment:
  sdk: ^3.12.2
  flutter: '>=3.44.0'

dependencies:
  flutter:
    sdk: flutter
  leelando_video: ^0.3.0
  leelando_video_media_kit: ^0.3.0
  leelando_video_fvp: ^0.3.0
  leelando_video_video_player: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
